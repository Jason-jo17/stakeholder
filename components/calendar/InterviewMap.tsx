export function InterviewMap({ events }: { events: any[] }) {
    return (
        <div className="h-[500px] w-full bg-muted/20 flex items-center justify-center rounded-lg border text-muted-foreground">
            Map View is under construction.
        </div>
    )
}
